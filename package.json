{
	"name": "${PACKAGE_NAME}",
	"version": "0.0.0",
	"description": "${PACKAGE_DESCRIPTION}",
	"author": "${AUTHOR_NAME} <${AUTHOR_EMAIL}>",
	"license": "${LICENCE}",
	"repository": {
		"type": "git",
		"url": "${GIT_URL}"
	},
	"bugs": {
		"url": "${BUGS_URL}"
	},
	"private": true,
	"types": "built/src/index.d.ts"
}
